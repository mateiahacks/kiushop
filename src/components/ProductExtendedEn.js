import Header from "./Header"

const ProductExtendedEn = () => {
    const { id } = useParams();

    return (
        <div>
            <Header />
            <h1>Product</h1>
        </div>
    )
}

export default ProductExtendedEn;